package com.example.aitranslate.model.node;

public enum TranslateNodeContentTypeEnum {
    /** A single plain text string. */
    PLAIN_TEXT,
    /** A JSON array of strings — each element translated individually. */
    JSON_ARRAY_STRING
}
